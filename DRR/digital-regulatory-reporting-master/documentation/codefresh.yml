version: '1.0'
build_version: 'v2'
steps:
  DeployDocsProdServer:
    title: Deploy prod DRR documentation Server Image
    type: deploy
    kind: kubernetes
    cluster: prod-cluster-private
    namespace: drr-docs-prod
    service: drr-docs
    candidate:
      image: regnosys/drr-docs:${{CF_RELEASE_TAG}}
      registry: regnosys-docker-registry

